name: 'run tests'
runs:
  using: "composite"
  steps:
    - name: install envsub
      shell: bash
      run: sudo apt-get update && sudo apt-get -y install gettext-base
    - name: run tests
      shell: bash
      run: |
        cd ./admission-webhook && ./run-ci.sh
